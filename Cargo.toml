[package]
name = "vm_dispatch"
version = "0.1.0"
edition = "2024"

[dependencies]
bitstruct = "0.1.1"
tramp = "0.3.0"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
opt-level = 3
strip = true
